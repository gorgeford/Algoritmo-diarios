//6- Creación de objetos Javascript uno

// Escribir una función que tome una cadena como argumento
// Crear un objeto que tenga una propiedad con clave 'clave' y un valor igual a la cadena
// Devolver el objeto

const CreateObject = (a) => {
    return {key : a};
}

console.log(CreateObject('n'));

