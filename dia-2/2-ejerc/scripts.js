// Escribir una función que tome una cadena (a) como argumento
// Extrae la primera mitad de la cadena
// Devuelve el resultado


const string = (a) => {

    return a.slice(0, a.length / 2);
}

console.log(string('devuelve'));